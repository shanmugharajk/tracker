# Tracker

A personal finance & tasks tracker application built in NextJs.

## Run the application

First, run the development server `bun dev`

Open [http://localhost:3000](http://localhost:3000) with your browser to see the application.
